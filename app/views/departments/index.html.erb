<div class='row-fluid'>
  <div class='span12 box bordered-box blue-border' style='margin-bottom:0;'>
    <div class='box-header blue-background'>
      <div class='title'><%= t(:listing)%> <%= t(:departments) %>  </div>
      <div class='actions'>
        <a href="#" class="btn box-collapse btn-mini btn-link"><i></i>
        </a>
      </div>
    </div>
    <div class='box-content box-no-padding'> 
      <div class='responsive-table'>
        <div class='scrollable-area'>
          <table class='data-table-column-filter table table-bordered table-striped' style='margin-bottom:0;'>
            <thead>
              <tr>
                 <th>ID</th>
                                  <th><%= t'.description' %></th>
                                 
                  
                <td>actions</td>
              </tr>
            </thead>
            <tbody>
              <% @departments.each do |department| %>
              <tr>
                <%if can? :show, Department %>
                <td><%=link_to department.id, department, :class => 'btn btn-info btn-mini'%></td>
                <%else%>
                <td><%= label department.id, department.id, :class => 'btn btn-info btn-mini'%></td>
                <%end%>
                
                
                    <% @model = department %> 
                  
                  <td><%= department.description %></td>
                                <%= render "layouts/index_links" %>
                <% end %>
              </tr>
            </tbody>
            <tfoot>
              <tr>
                <th>ID</th>
                                  <th>description</th>
                                <th>action</th>
              </tr>
            </tfoot>
          </table>
        </div>
      </div>
      <%= render 'layouts/action_links' %>
    </div>
  </div>
</div>
